<customUI xmlns="http://schemas.microsoft.com/office/2009/07/customui">
  <!--
    リボン: カスタム button の onAction は Main.RibbonCallback_hc_main に統一。
    tag は hc_main.invoke の action 文字列と同一（必須）。xlam の customUI へ手動取り込み想定。
  -->
  <ribbon>
    <tabs>
      <tab id="tabCSV" label="CSV Tool">

        <group id="DataAgg" label="データ集約">
          <button id="btnDataAgg" label="データ集約"
                  imageMso="CreateQueryFromWizard" size="large"
                  onAction="Main.RibbonCallback_hc_main"
                  tag="run_data_agg"
                  screentip="シナリオ作成/データ集約"
                  supertip="シナリオを作成し、そのシナリオに沿ってデータを集めExcelへ出力します。&#13;&#13;※注意：データは「文字列」として読み込まれます。" />
        </group>

        <group id="grpInput" label="ファイル操作">
          <button id="btnLoadCSV" label="CSV読込"
                  imageMso="FileOpen" size="large"
                  onAction="Main.RibbonCallback_hc_main"
                  tag="load_csv"
                  screentip="CSVファイルの読み込み"
                  supertip="ダイアログからCSVファイルを選択し、現在のブックまたは新規ブックに展開します。&#13;&#13;※注意：データは「文字列」として読み込まれます。" />

          <button id="btnSaveCSV" label="CSV保存"
                  imageMso="FileSaveAs" size="large"
                  onAction="Main.RibbonCallback_hc_main"
                  tag="save_csv"
                  screentip="CSVファイルとして保存"
                  supertip="現在のシートの内容をCSVファイル（UTF-8 BOM付）として保存します。" />
        </group>

        <group id="grpFile" label="結合/分割">
          <button id="btnMerge" label="・ファイル&#13;結合"
                  imageMso="AddWebPartConnection" size="large"
                  onAction="Main.RibbonCallback_hc_main"
                  tag="merge_csv"
                  screentip="CSVファイルの結合"
                  supertip="複数のCSVファイルを、現在のシートの末尾に追加・結合します。&#13;&#13;列の並びはシートに合わせて自動調整されます。&#13;&#13;元に戻す対象。" />

          <button id="btnSplit" label="ファイル&#13;分割"
                  imageMso="AdpDiagramRelationships" size="large"
                  onAction="Main.RibbonCallback_hc_main"
                  tag="split_csv"
                  screentip="CSVファイルの分割保存"
                  supertip="現在のシートを指定した行数で分割し、複数のCSVファイルとして保存します。" />

          <button idMso="GoTo" size="large"
                  screentip="ジャンプ"
                  supertip="指定したセル番地や、定義された名前の場所へ移動します。&#13;&#13;※ショートカットキー: Ctrl+G または F5" />
        </group>

        <group id="grpHeader" label="ヘッダ操作">
          <button id="btnSetHeader" label="・ヘッダ&#13;設定"
                  imageMso="EquationMatrixInsertRowBefore" size="large"
                  onAction="Main.RibbonCallback_hc_main"
                  tag="normalize_header"
                  screentip="複数行ヘッダの結合"
                  supertip="選択した複数行のヘッダを、1行のヘッダに結合・整形し、ﾃﾞｰﾀ領域も同様に結合・整形します。&#13;&#13;元に戻す対象。" />

          <button id="btnReleaseHeader" label="ヘッダ&#13;解除"
                  imageMso="EquationMatrixInsertRowAfter" size="large"
                  onAction="Main.RibbonCallback_hc_main"
                  tag="undo_last_action"
                  screentip="ヘッダ設定の解除（元に戻す）"
                  supertip="直前の「ヘッダ設定」操作を取り消し、元の状態に復元します。" />

          <button id="btnAddShukaHeader" label="・出荷履歴&#13;項目追加"
                  imageMso="InsertTableCaption" size="large"
                  onAction="Main.RibbonCallback_hc_main"
                  tag="insert_shuka_header"
                  screentip="出荷履歴DB用の項目名挿入"
                  supertip="アクティブなシートの1行目に行を挿入し、出荷履歴DB用項目名のひな形を追加します。&#13;&#13;元に戻す対象。" />
        </group>

        <group id="grpData" label="データ整形">

          <button id="btnCheckDup" label="重複&#13;チェック"
                  imageMso="ConditionalFormattingHighlightDuplicateValues" size="large"
                  onAction="Main.RibbonCallback_hc_main"
                  tag="check_duplicates"
                  screentip="重複データのチェック"
                  supertip="選択した列で重複している行を検出し、ハイライト表示します。" />

          <button id="btnDelRows" label="・空白行&#13;削除"
                  imageMso="DeleteRows" size="large"
                  onAction="Main.RibbonCallback_hc_main"
                  tag="delete_empty_rows"
                  screentip="空白行の一括削除"
                  supertip="データ部がすべて空白である行を一括で削除します。&#13;&#13;元に戻す対象。" />

          <button id="btnDelCols" label="・空白列&#13;削除"
                  imageMso="ColumnHideProject" size="large"
                  onAction="Main.RibbonCallback_hc_main"
                  tag="delete_empty_cols"
                  screentip="空白列の一括削除"
                  supertip="データ部がすべて空白である列を一括で削除します。&#13;&#13;元に戻す対象。" />

          <separator id="sep1" />

          <button id="btnDateYMD" label="・年月日 変換"
                  imageMso="DateAndTimeInsert" size="normal"
                  onAction="Main.RibbonCallback_hc_main"
                  tag="convert_date_ymd"
                  screentip="日付形式の変換 (YYYY/MM/DD)"
                  supertip="選択範囲の日付データを「YYYY/MM/DD」形式の文字列に変換します。&#13;&#13;元に戻す対象。" />

          <button id="btnDateYMDHM" label="・年月日時分 変換"
                  imageMso="DateAndTimeInsert" size="normal"
                  onAction="Main.RibbonCallback_hc_main"
                  tag="convert_date_ymd_hm"
                  screentip="日付形式の変換 (YYYY/MM/DD HH:MM)"
                  supertip="選択範囲の日付データを「YYYY/MM/DD HH:MM」形式の文字列に変換します。&#13;&#13;元に戻す対象。" />

          <button id="btnTrimming" label="・トリミング(文頭/文末)"
                  imageMso="Cut" size="normal"
                  onAction="Main.RibbonCallback_hc_main"
                  tag="trim_spaces"
                  screentip="文頭・文末の空白削除"
                  supertip="選択範囲のセルに対し、文頭および文末にある空白（半角・全角）を削除します。&#13;&#13;元に戻す対象。" />
        </group>

        <group id="grpOther" label="その他">

          <button id="btnUndo" label="アドイン&#13;元に戻す"
                  imageMso="Undo" size="large"
                  onAction="Main.RibbonCallback_hc_main"
                  tag="undo_last_action"
                  screentip="直前の操作を元に戻す"
                  supertip="直前に実行したアドインの操作を取り消し、元の状態に戻します。&#13;&#13;元に戻す対象は、メニュー先頭に&quot;・&quot;が付いている機能です。&#13;&#13;※注意：1回前の操作のみ有効です。" />

          <button id="btnHelp" label="ヘルプ&#13;"
                  imageMso="Help" size="large"
                  onAction="Main.RibbonCallback_hc_main"
                  tag="show_help"
                  screentip="ヘルプの表示"
                  supertip="アドイン設定および、操作マニュアルを表示します。" />

          <button id="btnCheckUpdates" label="更新&#13;確認"
                  imageMso="Info" size="normal"
                  onAction="Main.RibbonCallback_hc_main"
                  tag="check_for_updates"
                  screentip="バージョン確認（共有の catalog.json）"
                  supertip="HC_DEPLOY_ROOT 等で指定した共有上の catalog.json と VERSION.txt を照合します。xlam の customUI へ取り込み後に有効です。" />
        </group>

      </tab>
    </tabs>
  </ribbon>
</customUI>
