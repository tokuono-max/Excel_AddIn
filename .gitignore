# Nuitka batch build logs (see docs/Exe化（開発者向け）.md §3.5.2)
logs/nuitka/

# 第2段階に入る前の dist\CSV_Tool フルコピー（ロールバック比較用・容量大）
dist/CSV_Tool_baseline_pre_phase2/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual Env
.venv/
venv/

# Build
build/
dist/
# Root release/ sibling bats (nuitka/pack/diff) stay local; track full.bat wrapper + README.
# Leading slash = repo root only (do not ignore tools/release/).
# Canonical body: tools/release/full.bat
/release/*
!/release/full.bat
!/release/README.txt

# Logs
logs/
*.log

# Cursor (keep shared agent rules)
.cursor/*
!.cursor/rules/
!.cursor/rules/**

# Cursor Canvas（実体は canvases/。生成物のみ除外）
canvases/node_modules/
canvases/*.canvas.status.json

# VSCode (keep encoding hints for bat/vb)
.vscode/*
!.vscode/settings.json

# History
.history/

# Nuitka
*.build/
*.dist/
nuitka-crash-report.xml

# Qt
*.qm

# Temp
*.tmp
*.bak
_tmp*
.coverage
_pyright_*.txt
catalog_path.txt

# Local scratch Excel / Office lock at repo root
/*_test.xlsx
/~$*.xlam

# Excel temp
~$*.xlsm
~$*.xlsx

# OS
Thumbs.db
.DS_Store

# Dev: local packaged install root (tools/dev/packaged_root.local.bat.example is committed)
tools/dev/packaged_root.local.bat

# 現場用（インストール先直下）。リポジトリには置かない
/出荷履歴項目.json
config/catalog_path.txt
