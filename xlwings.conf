INTERPRETER_WIN = C:\Project\Python\Excel_AddIn\.venv\Scripts\pythonw.exe
INTERPRETER = C:\Project\Python\Excel_AddIn\.venv\Scripts\python.exe
USE_UDF_SERVER = False
DEBUG_UDFS = False

# --- Packaged EXE / Nuitka (optional) ---
# USE_PACKAGED_RUNPYTHON = True
#   When True, RunPython writes a UTF-8 snippet and calls INTERPRETER_WIN as a frozen-style EXE
#   (e.g. xlwings_short_runner.exe) with --script-file=... . Requires HC_INSTALL_ROOT and usually
#   HC_PACKAGED_DEPLOYMENT=1 in the environment (installer).
# INTERPRETER_WIN then points to the short-runner EXE under e.g. app\xlwings_short_runner\
