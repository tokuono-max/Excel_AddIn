# コードモジュール見直し — 改善一覧

- 作成日: 2026-09-11
- 対象: Excel_AddIn（CSV Tool）全体
- 注記: 頻度・トレードオフはコード経路と利用想定からの試算（実測テレメトリではない）

## 最優先の結論

正確性は結合キー正規化の不一致・抽出失敗の黙殺・シート名フォールバックが先。容量は logs/dist/_tmp 掃除で約4.5GB回収可能。構造は巨大モノリス分割が中期の本丸。

## 改善優先度一覧（スコア降順）

| # | 優先度 | 改善アイテム | 対象機能名 | 改善内容 | 改善度合い | 未改善時に起こり得る事象 | 頻度 | トレードオフ | 難易度 |
|---|--------|--------------|------------|----------|------------|--------------------------|------|--------------|--------|
| 1 | すぐに 10 | 正確性 | データ集約・結合キー／行マージ | 結合キー比較と行マージの正規化不一致 | 型ゆれ/'001等で行欠け・誤統合 | 同一キーなのに未結合／別キー扱い。出力行の欠落・重複・誤紐づけ | 高（キー型ゆれのあるシナリオで毎回） | 厳密正規化は既存シナリオの結果が変わる可能性。互換モードや段階移行が必要 | 中 |
| 2 | すぐに 10 | 正確性 | データ集約・抽出（Excel読取） | 抽出失敗の except→None/pass が空セルに見える | 原因不明の空欄・結合不一致 | 失敗が空欄に化け、結合失敗や誤った完成表を気付かず確定 | 中（破損・書式異常・巨大シート時） | 失敗をエラー停止にすると一括が途中終了。継続＋明示エラー印の両立が必要 | 中 |
| 3 | すぐに 9 | 正確性 | データ集約・シート指定読取 | シート名不一致時の active フォールバック | 別シートを黙って読む | 指定外シートのデータを集約し、正しそうに見える誤結果を出力 | 中（シート名変更・typo時は確実） | エラー化すると旧シナリオ（シート名曖昧運用）が止まる。警告付きフォールバック移行も可 | 低 |
| 4 | すぐに 9 | ファイル容量 | 開発環境（Nuitkaビルドログ） | logs/nuitka 約1.5GB の蓄積 | 約1.5GB即時回収 | ディスク逼迫・バックアップ遅延・ビルド失敗 | 高（ビルドするたび増加） | 削除すると過去ビルドの原因調査が難しくなる。直近N世代保持が現実的 | 低 |
| 5 | すぐに 9 | ファイル容量 | 開発環境（配布ビルド成果物） | dist/ 旧リリース＋重複ビルド約3.1GB | 2GB超削減見込み | ディスク逼迫・誤配布・旧版混同 | 高（リリース積み上げのたび） | 旧版ローカル比較ができなくなる。必要版はアーカイブ置き場へ退避 | 低 |
| 6 | すぐに 8 | ファイル容量 / スリム化 | リポジトリ衛生（一時ファイル・重複テスト） | ルート未追跡ゴミ (_tmp_* / .coverage / 重複テスト) | 誤コミット防止＋pytestノイズ排除 | ゴミの誤コミット・テスト収集失敗・レビューノイズ | 中（作業後に残存しやすい） | 調査用スクリプトも消える。再現手順が必要なら docs/tools へ正規化 | 低 |
| 7 | 高8 | 正確性 | データ集約・結合前加工（チェックラベル） | 加工チェックの部分文字列マッチ | ラベル変更で誤適用 | 意図しないトリム／日付変換が走り、キー不一致や値破壊 | 中（ラベル追加・文言変更時） | 厳密一致は表記ゆれに弱くなる。ID化すると設定移行コストが発生 | 低 |
| 8 | 高8 | 正確性 / スリム化 | 基盤層（core / svc 境界） | core → svc 循環依存 | 初期化順・Nuitka・単体テストの脆さ | 起動失敗・バンドル抜け・テストが壊れて回帰見逃し | 低〜中（リファクタ・配布ビルド時） | 層分割は広い import 差し替えが必要。短期は回帰リスクが上がる | 中 |
| 9 | 高8 | 速度 / スリム化 | データ集約コア／デバッグUI全体 | 巨大モノリス分割 (集約コア＋デバッグUI) | 変更リスク局所化 | 修正の副作用が広がり、回帰バグ・レビュー不能が常態化 | 高（機能追加のたび） | 分割作業自体が大規模・長期。途中はマージ衝突と二重メンテが増える | 高 |
| 10 | 高7 | 速度 | データ集約・Excel抽出（大シート） | シート全行 materialize のメモリ/I/O | 大シートで数百MB〜数GB・数秒〜数十秒 | メモリ逼迫・ハング・一括処理のタイムアウト | 高（大容量Excel利用時は毎回） | 範囲限定・ストリーム化は実装複雑。小シートでは全読込の方が単純で速い場合あり | 高 |
| 11 | 高7 | 速度 | データ集約・非表示行除外 | skip_hidden_rows 時の非 read_only 再オープン | 読込約2倍 | 一括処理が大幅遅延・体感タイムアウト | 高（非表示除外ON時は毎回） | read_only維持は非表示判定が不完全になり得る。正確性優先なら再オープンが必要 | 中 |
| 12 | 高7 | 正確性 / スリム化 | データ集約／UI／更新（横断） | 広域 except Exception の縮減 | 障害可視化・再発防止 | 本番障害の原因特定不能・誤った空結果の継続 | 高（例外経路は日常的） | 例外を厳格化するとCOM/Excelの一時失敗で処理が止まりやすくなる | 中 |
| 13 | 高7 | スリム化 / ファイル容量 | 旧実装アーカイブ (svc/Old) | svc/Old/ 死コード (git追跡) | 認知負荷削減 | 誤って旧APIを参照・検索ノイズ・レビュー誤誘導 | 低（触ったとき） | git履歴・タグに残るが、ローカル参照が不便になる | 低 |
| 14 | 高7 | スリム化 / 速度 | 処理サーバ起動 (svc_server) | svc_server の DEBUG Popen モンキーパッチ常設 | 本番ログノイズ除去 | ログ肥大・診断ノイズ・微小オーバーヘッド常時発生 | 高（子プロセス起動のたび） | 常時OFFだとスポーン調査が手間。環境変数でONにする形が無難 | 低 |
| 15 | 中5 | スリム化 | データ集約・polars任意読込 | _get_polars 三重定義 | 保守ずれ防止 | 片方だけ修正され挙動分岐・再現困難なバグ | 低（修正時のみ） | 共通化で循環importや遅延読込の設計が必要になる場合あり | 低 |
| 16 | 中5 | スリム化 | 日付変換 (YMD / HM) | svc_dt_ymd / svc_dt_hm のほぼ複製 | 数百行削減 | 片方だけ直して日付変換結果が機能間で不一致 | 低〜中（日付機能改修時） | 共通化で片方固有の仕様差を見失うリスク。差分を明示した共通化が必要 | 中 |
| 17 | 中5 | 速度 / スリム化 | データ集約・シナリオ編集UI | UIシナリオ編集の過剰 deepcopy | 項目多いとUI鈍化 | 編集操作のカクつき・入力遅延・誤操作誘発 | 中（大規模シナリオ編集時） | 浅いコピーは副作用で編集内容が汚染される危険。参照管理の設計が要る | 中 |
| 18 | 中5 | ファイル容量 / スリム化 | 配布パッケージ／依存管理 | 依存の肥大 (lockと配布の乖離) | 数十〜数百MB級の可能性 | 配布ZIP肥大・導入時間増・不要DLL混入 | 中（配布ビルドごと） | polars等を外すと大ファイル高速経路が弱まる。任意依存のままが妥当な場合あり | 中 |
| 19 | 中4 | ファイル容量 | ドキュメント管理 | ドキュメント版二重・docx/PDF重複 | 数MB〜10MB | 版の取り違え・説明齟齬・容量浪費 | 低（ドキュメント参照時） | docx削除は社内提出物ワークフローと衝突し得る。生成物置き場分離が安全 | 低 |
| 20 | 中4 | 速度 / スリム化 | データ集約・結合書込ホットパス | 結合ダンプ／診断コードの本番常駐 | ホットパス可読性向上 | 保守困難・微小性能ロス・診断フラグ誤ON時の大量出力 | 低（通常OFF）／高（誤ON時） | 外出しすると現場診断の即応性が下がる。フラグ付き別モジュールが妥協点 | 中 |
| 21 | 中4 | 速度 | データ集約UI・前面化／ロック再試行 | ui_data_agg の processEvents / 多重 QTimer | 再入・ちらつき低減 | ダイアログちらつき・再入バグ・稀なフリーズ感 | 中（前面化・Excel競合時） | 状態マシン化は実装・検証コスト高。現状の簡易再試行は実装が短い | 中 |
| 22 | 低3 | スリム化 / ファイル容量 | 自動テスト／CI | テスト肥大・重複シナリオの整理 | CI時間短縮 | CI遅延・重複テストのメンテ漏れで偽緑 | 中（CI実行のたび） | 統合しすぎるとカバレッジ穴。回帰の重要ケースは残す必要がある | 中 |

## 推奨着手順

### すぐ（容量＋正確性の止血）
1. logs/nuitka・dist旧版・_tmp_* / .coverage / 重複テスト削除＋gitignore
2. シート不存在をエラー化・抽出失敗をログ＋区別可能値に
3. 結合キー正規化方針を仕様文書と突合

### 高〜中（構造と性能）
4. svc/Old削除・DEBUGパッチ除去・_get_polars一本化
5. materialize範囲制限・hidden二重オープン削減・except縮減
6. svc_data_agg / ui_data_agg_debug の分割

## 頻度の読み方

| 表記 | 意味 |
|------|------|
| 高 | 該当機能利用時にほぼ毎回、または日常運用で頻発 |
| 中 | 特定条件（大ファイル・設定ON・改修時など）で発生 |
| 低 | 稀、または触ったとき／誤設定時のみ |
