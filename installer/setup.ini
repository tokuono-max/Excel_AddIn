; 薄いインストーラ実行時の上書き（CSV_Tool_Setup.exe と同じフォルダに置く）。
; 詳細: docs\インストーラ化（開発者向け）.md §2.6
; DeployRoot = catalog.json があるフォルダ（末尾の \ は不要）。空ならコンパイル時 SHAREPAYLOAD。
; InstallDir = 空ならインストーラ既定。BOM 付き UTF-8 は使わない。
[Setup]
DeployRoot=C:\UNC\release
InstallDir=
