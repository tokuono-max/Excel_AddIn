# 開発用まとめインストール（意図は各レイヤファイルを正とする）
#   pip install -r requirements.lock.txt
# 配布 ZIP サイズは Nuitka 同梱結果で決まり、本ファイルの行＝ZIP ではない。
# #18: debugpy / ruff / PyInstaller 系は runtime から除外。polars は optional 維持。
-r requirements-runtime.txt
-r requirements-optional.txt
-r requirements-dev.txt
-r requirements-build.txt
